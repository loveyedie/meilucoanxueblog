#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_regexpu@1.3.0@regexpu/bin/regexpu" "$@"
  ret=$?
else 
  node  "$basedir/../../../_regexpu@1.3.0@regexpu/bin/regexpu" "$@"
  ret=$?
fi
exit $ret
